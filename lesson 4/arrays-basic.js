var names = ['Steve', 'Daniel', 'Jim'];
var score = [26000, 18000, 12000];

console.log(getScore(1));
console.log(getScore(2));

function findPlayer(name){
  return ''; //index of player with name from names
}

function getScore(index){
  return names[index] + ' score is ' + score[index];
}

//Shift & Unshift - Beginning of Array
//Push & Pop - The End of Array
